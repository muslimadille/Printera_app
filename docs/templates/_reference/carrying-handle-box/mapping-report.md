# Carrying Handle Box — Mapping Report (approved)

All audits below were generated by `auditCarryMapping()` and locked by
`src/lib/carryingHandleBoxEngine.test.ts`.

## Contract

- X boundaries depend on `L`, `D`, `G` only — **never on H**
- Y boundaries depend on `D`, `H` only — **never on L**
- Footprint = `(X5, Y3)` exactly (no duplicated math)

## Test 1 — Reference (L=300, D=150, H=180)

| Boundary | Value |
|----------|-------|
| X0..X5   | 0, 30, 330, 480, 780, 930 |
| Y0..Y3   | 0, coverTotal(150), +180, +bottomFlapMax(150) |
| Footprint| 930 × 435 |

Result: **PASS** (1 piece per sheet)

## Test 2 — Approved (L=320, D=160, H=170)

| Boundary | Value |
|----------|-------|
| X0..X5   | 0, 30, 350, 510, 830, 990 |
| Footprint| 990 × 442 |

Result: **PASS**

## Test 3 — Edge (L=330, D=140, H=160)

| Boundary | Value |
|----------|-------|
| X0..X5   | 0, 30, 360, 500, 830, 970 |
| Footprint| 970 × ~426 |

Result: **PASS**

## Independence checks (locked)

- `carryXBoundaries(L,D,H=100) === carryXBoundaries(L,D,H=400)` ✅
- `carryYBoundaries(L=200,D,H) === carryYBoundaries(L=500,D,H)` ✅

## Audit columns (every row must pass)

For each component the audit reports:
`component | expectedSize | currentSize | formula | pass`.

All rows green across the 3 sizes — see vitest output for
`src/lib/carryingHandleBoxEngine.test.ts`.
